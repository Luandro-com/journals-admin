mutation($issueId: ID!) {
	publishIssue(issueId: $issueId) {
    id
    key
    title
    volume
    number
    year
    body
    evaluationPeriod
    publicationPrediction
    contact
    publishedCall
    published
    startCall
    endCall
  }
}